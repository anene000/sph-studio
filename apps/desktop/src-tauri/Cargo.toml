[package]
name = "sph-studio"
version = "0.0.1"
description = "SPH Studio desktop shell"
authors = ["anene000"]
edition = "2021"

[lib]
name = "sph_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# U19: launch the PyInstaller-built Python (FastAPI) backend as a sidecar.
tauri-plugin-shell = "2"
